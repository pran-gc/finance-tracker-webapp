# Local environment variables for Finance Tracker (DO NOT commit secrets to VCS)
# Replace the placeholder with your Google OAuth client ID (Web application client).
# For client-side (gapi) sign-in we only need the public client ID.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=868358388617-se3lske3jhooj01sufgelen6aa9m5tb3.apps.googleusercontent.com

# Optional: for documentation or future server-side flows
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# If you are testing from an ngrok or other tunnel add the origin below for your own reference
# (The important step is to add the same origin to the Google Cloud OAuth "Authorized JavaScript origins" section.)
# Example:
# NEXT_PUBLIC_ALLOWED_ORIGIN=https://cunning-panda-nearby.ngrok-free.app
